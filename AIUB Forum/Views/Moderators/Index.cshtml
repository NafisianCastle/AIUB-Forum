@model IEnumerable<AIUB_Forum.Models.Database.Moderator>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.User.Location)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.User.Location)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ModeratorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ModeratorId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ModeratorId })
        </td>
    </tr>
}

</table>
